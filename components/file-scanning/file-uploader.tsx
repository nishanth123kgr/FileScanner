"use client"

// Fix the import path to correctly point to the modular folder structure
import { FileUploader as FileUploaderComponent } from "./file-uploader/index"

// Re-export the component as the default export
export default FileUploaderComponent
